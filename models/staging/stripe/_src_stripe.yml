version: 2

sources:
  - name: stripe
    database: DBT_DB
    schema: DBT_JMELIK_JAFFLE_SHOP

    tables:
      - name: STRIPE_PAYMENTS
        config:
          loaded_at_field: CREATED
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 24
              period: hour